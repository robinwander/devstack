/** Simple wordmark — no icons, no decoration. The product name IS the logo. */
export function LogoMark() {
  return (
    <span className="text-sm font-semibold tracking-tight text-ink select-none">
      devstack
    </span>
  );
}
